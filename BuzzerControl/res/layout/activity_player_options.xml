<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".PlayerOptionsActivity" >

	<EditText
	    android:id="@+id/playerEdit"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_alignParentLeft="true"
	    android:layout_alignParentTop="true"
	    android:layout_marginLeft="32dp"
	    android:layout_marginTop="32dp"
	    android:ems="10" >

    	<requestFocus />
	</EditText>
	
	<Button
	    android:id="@+id/playerUpdate"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_alignTop="@+id/playerEdit"
	    android:layout_marginLeft="19dp"
	    android:layout_toRightOf="@+id/playerEdit"
	    android:text="@string/update"
	    android:onClick="updateName" />

	<TextView
	    android:id="@+id/playerRename"
	    android:layout_width="wrap_content"
	    android:layout_height="wrap_content"
	    android:layout_above="@+id/playerUpdate"
	    android:layout_alignLeft="@+id/playerEdit"
	    android:text="@string/player_rename"
	    android:textAppearance="?android:attr/textAppearanceLarge" />

</RelativeLayout>